# Changelog

- 修复知识库外部依赖导致的默认启动回归：未配置 MySQL 时回退本地 JSON，Milvus 未配置、连接失败或投影失败时降级为 MySQL 关键词检索，并记录明确 warning。

- 知识库改为 MySQL 8 权威存储与 Milvus 向量索引，使用 Eino Embedder/Indexer/Retriever 组件接口完成中文 QA 向量化和检索，并通过 RRF 融合关键词与向量召回；支持旧 JSON 幂等迁移和启动时全量向量一致性重建。

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的记录方式。

## [Unreleased]

### Added

- 面试改为优先从领域知识库 QA 出题；回答由配置的大模型按标准答案做语义相似度评分，低于 75 分时保留当前进度并按所选面试官风格细化追问，达到阈值后再进入下一题。
- 英语面试模式现已覆盖英文技术题、英文追问与评价、`en-US` 实时语音识别和豆包英文 TTS；模型不可用时仍提供全英文领域题与本地反馈。
- 参考 Project AIRI 的身体、声音与上下文分层，为实时面试接入本地 VRM 3D 面试官形象。
- 形象新增空闲、聆听、思考、说话状态，并与火山实时 ASR、答案评估和豆包 TTS 生命周期联动。
- VRM 形象新增持续呼吸与重心摆动，并为聆听、思考、说话状态加入点头、侧头和双臂手势，状态切换使用平滑骨骼插值。
- 修正 Seed-san VRM 的默认朝向，面试形象现在正面朝向用户而不是显示后脑勺。
- 本地打包 VRM 官方 Seed-san 示例模型及 VRM Public License 1.0 署名说明，离线环境仍可渲染。
- 移除浏览器 SpeechRecognition 与 OpenAI-compatible 整段录音转写，改为火山引擎大模型流式语音识别；支持 PCM 实时上行、临时文本和最终文本，并使用独立的语音凭据配置。
- 新增豆包 TTS 面试官朗读，覆盖开场、题目、追问和反馈；支持静音、重播以及录音前自动停播，形成 ASR + TTS 双向语音面试。
- 计算机领域 Skill 扩展至 C、C#、JavaScript、TypeScript、Rust、SQL、汇编等多种语言，并提供菜鸟教程资料同步 Skill。
- 准备页的领域与面试官选择器支持搜索和键盘导航，便于在扩展后的 Skill 列表中快速定位。
- 面试回答接口新增用户意图识别，支持提示、澄清、重复题目和跳过；非正式作答不会推进题目。
- 已配置大模型时，意图识别可理解偏题、闲聊和状态表达，并生成面试官式回应把对话拉回当前题。
- 前端面试页新增当前题内的多轮提示/澄清对话展示，并在正式作答后归档到对应题卡。
- Electron 后端启动时会在默认端口被占用后自动选择可用端口，避免重复启动时报 `address already in use`。
- Electron 主进程通过同步运行时配置向前端注入实际 API 地址与令牌，Skill 列表加载不再依赖固定端口或 preload 环境变量。
- Skill 目录接口调整为公开只读，避免本地令牌刷新或旧后端进程残留导致准备页 Skill 加载失败。
- 已配置模型时优先动态生成每场面试题，失败时回退内置题库；回答链路压缩到 20 秒内并在前端显示处理流程。
- 新增可选视频面试入口、摄像头预览和中英文语音输入；ModelScope CAM++ 声纹模型作为后续服务端适配方向。
- 开发模式下 Electron 直接启动临时构建的后端二进制，并按进程组清理，避免 `go run` 子进程残留占用端口。
- 语音输入新增录音转写兜底，并在 Electron 中显式放行麦克风/摄像头权限，解决 Web Speech API 不可用时无法语音输入的问题。
- Web Speech 识别改为实时把临时识别结果写入回答框，识别中断时保留已识别文字，由用户确认后再发送。
- 模型设置新增独立 `Speech Model`，录音转写不再复用聊天模型，并优化上游转写失败时的错误提示。
- 新增语音转写流式接口 `/speech/transcriptions/stream`，上游支持 SSE 时将转写增量实时写入回答框，不支持时自动退化为最终文本。
- 语音转写流兼容 `choices[].delta.content`、CRLF 事件分隔和结束事件携带最终文本等服务商差异。
- 语音转写增加双层兜底：流式请求被服务商拒绝时，服务端和前端都会自动退回普通转写，避免 `stream=true` 不兼容导致语音输入失败。

### Fixed

- 修复 Electron 内容安全策略写死开发端口、拦截动态本地实时语音 WebSocket 的问题，并新增真实 WebSocket 握手回归测试。
- 调试模式退出时同步终止 Go 后端，避免残留进程占用本地端口。

### Planned

- 持久化面试历史与题库管理。
- 为扫描版 PDF 增加可选 OCR。
- 建立 GitHub Actions 跨平台构建与自动发布流水线。

## [0.2.0] - 2026-06-23

### Added

- 新增 Echo、Atlas、Vera、Socrates 四个面试官风格 Skill，并将风格提示、评价重点和反馈语气接入 Eino Agent。
- 新增可扩展行业/领域 Skill Manifest；当前提供计算机基础、Golang、Java、Python、C++。
- 新增 6 道计算机基础公共题，可选择混入语言专项面试。
- 新增 `computer-foundation` 与四个语言独立知识库，支持 QA 持久化、检索、手动新增和出题次数统计。
- 面试创建时自动把实际出题记录到对应知识库。
- 新增学习中心，实时聚合 Go、Java、Spring、Python、C++ 权威 Feed。
- 新增官方文档、课程和视频入口，以及本地学习清单。
- 新增 Skill、知识库和学习资源 REST API。

### Changed

- 准备页从固定语言选项升级为动态行业/领域 Skill 选择。
- 面试页和复盘页展示实际面试官 Skill 与领域 Skill。
- 版本升级至 `0.2.0`。

### Security

- Feed 条目只接受与权威来源同域的 HTTPS 链接，页面内容始终按纯文本展示。

## [0.1.0] - 2026-06-22

### Added

- 初始化 Go、Eino、Electron、React 工程。
- 新增 PDF / DOCX 简历解析、关键词提取与 10 MB 上传限制。
- 新增 Golang、Java、Python、C++ 共 24 道标准化题目。
- 新增简历相关性排序、面试状态机、计时和进度管理。
- 新增 Eino OpenAI-compatible 模型评价与本地评分降级。
- 新增逐题反馈、综合报告和标准答案复盘。
- 新增模型设置、本机凭据持久化和连接测试。
- 新增 Electron 本地服务托管、上下文隔离和随机访问令牌。
- 新增中文需求文档、接口文档、README 与测试用例。

### Security

- Go API 仅监听回环地址。
- Electron 每次启动生成随机 API 访问令牌。
- 简历临时文件解析后删除，模型 API Key 文件权限设为 `0600`。
